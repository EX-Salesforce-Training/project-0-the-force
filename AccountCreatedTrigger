trigger AccountCreatedTrigger on Account (after insert) {
   
    AccountCreatedTriggerHandler acctHand = new AccountCreatedTriggerHandler();
    
    if(Trigger.isInsert){
        acctHand.afterInsertMethod(trigger.new);
    }
    
}
